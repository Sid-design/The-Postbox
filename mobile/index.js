/**
 * @format
 */
import 'expo-dev-client';
import 'react-native-gesture-handler';

import { registerRootComponent } from 'expo';
import App from './App';

// Expo will handle registering the component as "main" automatically.
registerRootComponent(App);
